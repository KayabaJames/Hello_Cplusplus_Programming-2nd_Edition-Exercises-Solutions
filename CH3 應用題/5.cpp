#include <bits/stdc++.h>

using namespace std;

int main()
{
    double upper, lower, height, area;

    cin >> upper >> lower >> height;
    area = (upper + lower) * height / 2;
    cout << area << endl;

    return 0;
}
