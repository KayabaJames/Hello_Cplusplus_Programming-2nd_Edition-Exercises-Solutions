#include <bits/stdc++.h>

using namespace std;

int main()
{
    int a, b, c;
    cout << "請輸入a, b, c三整數：";
    cin >> a >> b >> c;
    cout << (a + b + c) << " " << (a * b * c) << endl;
    return 0;
}
