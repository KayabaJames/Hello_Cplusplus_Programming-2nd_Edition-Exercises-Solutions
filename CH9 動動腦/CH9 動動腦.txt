(P.9-19)
1. 範例9.4.2 傳址呼叫的程式碼中，兩數交換的swap函式改用^=運算子時，第17~19行程式碼需改寫成什麼？

Ans: ^= 是「互斥複合運算子」；根據Ch3 P.3-19的介紹，
我們可以知道是使用
void swap(int * i, int * j)
{
    // 互斥複合運算子
    *i^=*j^=*i^=*j;
}
就能達到兩數交換的功能。
